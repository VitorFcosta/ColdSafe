[pytest]
addopts = --cov=backend/app --cov-branch --cov-report=term-missing --cov-report=html --cov-fail-under=80
testpaths = backend/tests
pythonpath = .
markers =
    integration: requires disposable local infrastructure
